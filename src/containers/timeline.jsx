import React from "react";

const timeline = () => {
  const images = ["an", "array", "of", "images"];
  return (
    //create timeline
    <div>
      <h1>Timeline</h1>
    </div>
  );
};

const timelineImages = (image) => {
  <div></div>;
};
